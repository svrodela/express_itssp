<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8" />
  <title><%= producto.nombre %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="p-4">
  <div class="container">
    <h1><%= producto.nombre %></h1>

    <div class="row mb-3">
      <div class="col-md-4">
        <% if (producto.imagen) { %>
          <img src="<%= producto.imagen %>" alt="imagen" class="img-fluid">
        <% } else { %>
          <div class="border p-4 text-center text-muted">Sin imagen</div>
        <% } %>
      </div>
      <div class="col-md-8">
        <h4>Precio: $<%= Number(producto.precio).toFixed(2) %></h4>
        <p><%= producto.descripcion %></p>

        <a href="/productos" class="btn btn-secondary">Volver</a>

        <form action="/productos/<%= producto.id %>/delete" method="POST" style="display:inline" onsubmit="return confirm('¿Eliminar este producto?')">
          <button class="btn btn-danger">Eliminar</button>
        </form>
      </div>
    </div>
  </div>
</body>
</html>
